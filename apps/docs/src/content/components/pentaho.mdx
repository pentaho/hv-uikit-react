# Widget Components

The widgets package contains a set of UI components designed to address the unique needs of our products,
that don't quite fit into the [core package](.) guidelines.

## Usage

```sh
npm install @pentaho/uikit-react-widgets
```

```tsx
import { HvCanvasSidePanel } from "@pentaho/uikit-react-widgets";
```
