---
title: "Cards"
---

import { CodeBlock } from "../../../components/code/CodeBlock";
import selectable from "./Selectable?raw";
import selectableCentered from "./SelectableCentered?raw";
import selectableCheck from "./SelectableCheck?raw";
import selectableCheckCentered from "./SelectableCheckCentered?raw";
import selectableRadio from "./SelectableRadio?raw";
import selectableRadioCentered from "./SelectableRadioCentered?raw";

### Selectable card

A card that can be clicked to toggle a selected state.

<CodeBlock code={selectable} />

### Selectable card centered

A selectable card with the content centered inside.

<CodeBlock code={selectableCentered} />

### Selectable radio card

A card using radio-style selection—only one card in the group can be selected at a time.

<CodeBlock code={selectableRadio} />

### Selectable radio card centered

A selectable radio card with centered content layout.

<CodeBlock code={selectableRadioCentered} />

### Selectable checkbox card

A card with a checkbox to support multi-selection within a group.

<CodeBlock code={selectableCheck} />

### Selectable checkbox card centered

A selectable checkbox card with centered content layout.

<CodeBlock code={selectableCheckCentered} />
