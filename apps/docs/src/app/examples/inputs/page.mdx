---
title: "Inputs"
---

import { CodeBlock } from "../../../components/code/CodeBlock";
import copyButton from "./CopyButton?raw";
import dropdownPrefix from "./DropdownPrefix?raw";
import dropdownSuffix from "./DropdownSuffix?raw";
import dropdownTreeview from "./DropdownTreeview?raw";
import editableValue from "./EditableInput?raw";
import labelSide from "./LabelSide?raw";
import leadingIcon from "./LeadingIcon?raw";
import tagsDropdown from "./TagsDropdown?raw";
import tagsInputDropdown from "./TagsInputDropdown?raw";

### Leading icon

Input field with a leading icon adornment to visually hint at the expected input type.

<CodeBlock code={leadingIcon} />

### Copy button

Input field with a trailing copy-to-clipboard button adornment.

<CodeBlock code={copyButton} />

### Label left side

Input field with the label positioned to the left rather than above the input.

<CodeBlock code={labelSide} />

### Dropdown prefix

Input field with a dropdown selector as a prefix, useful for type or category selection.

<CodeBlock code={dropdownPrefix} />

### Dropdown suffix

Input field with a dropdown selector as a suffix, useful for unit or format selection.

<CodeBlock code={dropdownSuffix} />

### Dropdown with treeview

Input field combined with a treeview dropdown panel for hierarchical value selection.

<CodeBlock code={dropdownTreeview} />

### Tags dropdown

Multi-select input where chosen values appear as removable tags, with a dropdown for selection.

<CodeBlock code={tagsDropdown} />

### Tags input dropdown

Combined free-text input and dropdown panel that produces a list of tag values.

<CodeBlock code={tagsInputDropdown} />

### Inline editable text

A read-only text value that switches to an editable input on click.

<CodeBlock code={editableValue} />
